"""
타입 계약 — 품질 마감(표기·중복·거시·자막) (라운드㉗)
검증: python -m mypy --strict --ignore-missing-imports api-contract-polish.py

작고 가시적인 거슬림 정리 — 외부 API/DB/이벤트 변경 없음(내부 표기/정리 로직).
P1 수치 표기 · P2 근사 중복 헤드라인 dedup · P3 거시 지표명/단위 정리 · P4 자막 줄바꿈.
"""
from __future__ import annotations

# ── P1 수치 표기 ──
# 시나리오·내레이션의 chart 문장 수치를 사람이 읽는 형태로.
#   close  : 416500.0  → "416,500"(천단위·소수 제거)
#   change : 4.39      → "+4.39"(부호 포함) → 문장은 "…종가 416,500원, 등락률 +4.39%"
PRICE_FMT_NOTE = "close=f'{round(v):,}', change=f'{v:+.2f}'. 값 자체 불변(표기만)."

# ── P2 근사 중복 헤드라인 ──
# 정규화 완전일치(현행) + **토큰 자카드 유사도** 임계 이상이면 중복으로 병합(최신 우선).
DEDUP_JACCARD = 0.4  # 말줄임 앞 핵심부 토큰 자카드. 이 이상이면 같은 사건으로 병합(패러프레이즈까지).
# 참고: 동의어 수준 의미 중복(스튜디오↔연구거점)은 임베딩 필요 → ADR 0006 제외, 후속.

# ── P3 거시 지표명·단위 정리 ──
# 기준연도 등 노이즈 단위 제거 + 지표명 축약(표시용). 값은 사실 슬롯 불변.
MACRO_UNIT_DROP = ("=100", "1982-84", "2020=")  # 이런 노이즈 포함 단위는 표시에서 생략
MACRO_NAME_SHORT: dict[str, str] = {
    "원달러환율(매매기준율)": "원/달러 환율",
    "연방기금금리(실효)": "미 기준금리",
    "한국은행 기준금리": "한은 기준금리",
    "미 소비자물가지수": "미 CPI",
    "소비자물가지수": "CPI",
}

# ── P4 자막 줄바꿈 ──
# 긴 자막은 말줄임 대신 2줄 줄바꿈(drawtext 실제 개행). 배지는 말줄임 완화.
CAPTION_WRAP_WIDTH = 18  # 대략 글자 수/줄
CAPTION_MAX_LINES = 2
