# 20260723 — 요약: 품질 마감 (수치 표기·중복·거시·자막)

- **Task**: `doc/design/content/20260723-task-polish.md` (라운드㉗)
- **작업**: /run(builder→sync) · 날짜 2026-07-23 · 브랜치 main · **로컬 전용**(실 컨테이너 스택)
- **상태**: 완료·검증. 작고 가시적인 거슬림 4가지 정리. 값=사실 불변(표기만).

## 변경사항

- **agent `script/builder.py`**:
  - **P1 수치 표기**: chart 슬롯 `close=f"{round(v):,}"`(천단위)·`change=f"{v:+.2f}"`(부호) → "종가 416,500원, 등락률 +4.39%". 값 불변.
  - **P3 거시 정리**: `_macro_disp`·`_MACRO_NAME_SHORT`(미 소비자물가지수→미 CPI 등)·`_MACRO_UNIT_DROP`(1982-84=100 등 노이즈 단위 생략). 값 슬롯 불변.
- **research `domains/research/service.py`**:
  - **P2 근사 중복 dedup**: 말줄임(…) 앞 핵심부 토큰 자카드 ≥ 0.4면 병합(최신 우선). 완전일치→패러프레이즈까지.
- **video-assembly `assemble.py`**:
  - **P4 자막 줄바꿈**: `_wrap`(공백 우선·글자 수, 최대 2줄, 초과 말줄임), `_esc` 개행 유지, 자막 y=h-280, 배지 shorten 34→44.

## API/타입 변경

- 없음(내부 표기/정리 로직). 계약 `api-contract-polish.py`(규칙 문서).

## 검증 (실 컨테이너 스택)

- job#34 시나리오: chart "현대차 종가 416,500원, 등락률 +4.39%"(P1), fact UX 상하이 **중복 2건→1건**(P2), macro "미 CPI 332.568, CPI 116.52 수준입니다"(P3).
- 프레임: 훅 자막 "현대차, 노사 대립으로 생산 차질 / 발생" **2줄**, 배지 전체 표기(P4).
- 단위테스트: agent 3(수치 표기 기대값 갱신 — 의도 불변)·video-assembly 3·news-feed 6 통과.

## 특이사항 (설계 대비·후속)

- **이탈**: 자카드 임계 0.6→**0.4**(전체 원제목 대신 말줄임 앞 핵심부 비교로 조정). 동의어 수준 의미 중복(스튜디오↔연구거점)은 임베딩 필요 → ADR 0006 제외, 후속.
- **후속**: 거시 값 소수 자릿수(332.568) 정리, 배경 컷 전환·수치 scale 팝(연출), 구간 TTS 병렬화.
- 커밋: 아직(사람 게이트 — `/commit`).